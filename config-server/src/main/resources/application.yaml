server:
  port: 8888
  tomcat:
    max-http-header-size: 8192000



spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo