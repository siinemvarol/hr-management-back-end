server:
  port: 9093

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: hrcommentdb
      username: ${MONGO_USERNAME}
      password: ${MONGO_PASS}
  rabbitmq:
    host: localhost
    port: 5672
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASS}