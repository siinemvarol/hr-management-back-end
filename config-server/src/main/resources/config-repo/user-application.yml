server:
  port: 9095

  tomcat:
    max-http-header-size: 819200

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: hruserdb
      username: ${MONGO_USERNAME}
      password: ${MONGO_PASS}
  rabbitmq:
    host: localhost
    port: 5672
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASS}
