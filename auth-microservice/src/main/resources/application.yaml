server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: "jdbc:postgresql://localhost:5432/AuthDB"
    username: "postgres"
    password: "12345678"
  jpa:
    hibernate:
      ddl-auto: update
  rabbitmq:
    host: localhost
    port: 5672
    username: java8
    password: root
    #TODO rabbit mq diğer servicelere de eklenmesi gerekiyor
